**** PROPERTIES SKAFFOLDER ****
{
    "forEachObj": "resource",
    "overwrite": false,
    "_partials": []
}
**** END PROPERTIES SKAFFOLDER ****
import {{capitalize resource.name}}ModelGenerated from "./generated/{{capitalize resource.name}}ModelGenerated";

const customModel = {
  
  /**
   * Customize here your schema with custom attributes
   * 
   * EXAMPLE:
    
    init() {
      let schema = {{capitalize resource.name}}ModelGenerated.init();
  
      schema.add({
        extraCustomField: Object
      });
    },
     
   */


  /**
   * Override here your custom queries
   * EXAMPLE:
   *
   
    async get(id) {
      console.log("This is my custom query");
      return await {{capitalize resource.name}}ModelGenerated.getModel().findOne({ _id: id });
    }

   */

};

export default {
  ...{{capitalize resource.name}}ModelGenerated,
  ...customModel
};
